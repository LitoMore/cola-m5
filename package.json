{
  "name": "cola-m5",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "workspaces": [
    "plugin"
  ],
  "scripts": {
    "build": "npm --workspace plugin run build",
    "typecheck": "npm --workspace plugin run typecheck"
  }
}
